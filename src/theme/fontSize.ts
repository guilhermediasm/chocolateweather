import { moderateScale } from '~/utils';

const fontSize: FontSize = {
  small: `${moderateScale(12)}px`,
  mediumSmall: `${moderateScale(16)}px`,
  medium: `${moderateScale(18)}px`,
  big: `${moderateScale(24)}px`,
};

export default fontSize;
