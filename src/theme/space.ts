import { moderateScale } from '~/utils';

const space = {
  small: `${moderateScale(10)}px`,
  pageSmall: `${moderateScale(20)}px`,
  page: `${moderateScale(24)}px`,
  large: `${moderateScale(51)}px`,
};

export default space;
